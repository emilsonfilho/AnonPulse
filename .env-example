# Banco (usado pelo docker-compose)

DB_NAME=anonpulse
DB_USER=admin
DB_PASSWORD=admin

# URL usada pela aplicação (sqlite:///database/anonpulse.db para SQLite ou postgresql://admin:admin@postgres:5432/anonpulse para PostgreSQL)

DATABASE_URL=postgresql://admin:admin@postgres:5432/anonpulse
